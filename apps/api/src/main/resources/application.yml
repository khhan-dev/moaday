spring:
  application:
    name: moaday-api
  datasource:
    url: ${DATABASE_URL:jdbc:h2:file:./data/couponwith;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE}
    username: ${DATABASE_USERNAME:sa}
    password: ${DATABASE_PASSWORD:}
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
  flyway:
    enabled: true
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 21MB
  mail:
    host: ${MAIL_HOST:localhost}
    port: ${MAIL_PORT:1025}
    username: ${MAIL_USERNAME:}
    password: ${MAIL_PASSWORD:}
    properties:
      mail:
        smtp:
          auth: ${MAIL_SMTP_AUTH:false}
          starttls:
            enable: ${MAIL_STARTTLS_ENABLED:false}
            required: ${MAIL_STARTTLS_REQUIRED:false}
          connectiontimeout: ${MAIL_CONNECTION_TIMEOUT_MS:10000}
          timeout: ${MAIL_READ_TIMEOUT_MS:10000}
          writetimeout: ${MAIL_WRITE_TIMEOUT_MS:10000}

server:
  port: ${PORT:8080}
  error:
    include-message: never

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      probes:
        enabled: true

couponwith:
  jwt:
    secret: ${JWT_SECRET:coupon-with-local-development-secret-change-before-production-2026}
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:5173}

moaday:
  web-base-url: ${MOADAY_WEB_BASE_URL:http://localhost:3000}
  security:
    login:
      maximum-attempts: ${LOGIN_MAXIMUM_ATTEMPTS:5}
      lock-seconds: ${LOGIN_LOCK_SECONDS:900}
    password-reset:
      expiry-minutes: ${PASSWORD_RESET_EXPIRY_MINUTES:30}
      request-cooldown-seconds: ${PASSWORD_RESET_COOLDOWN_SECONDS:60}
    email-verification:
      expiry-minutes: ${EMAIL_VERIFICATION_EXPIRY_MINUTES:30}
      request-cooldown-seconds: ${EMAIL_VERIFICATION_COOLDOWN_SECONDS:60}
  mail:
    from: ${MAIL_FROM:no-reply@moaday.local}
    outbox:
      interval-ms: ${MAIL_OUTBOX_INTERVAL_MS:10000}
      initial-delay-ms: ${MAIL_OUTBOX_INITIAL_DELAY_MS:5000}
      batch-size: ${MAIL_OUTBOX_BATCH_SIZE:20}
      max-attempts: ${MAIL_OUTBOX_MAX_ATTEMPTS:5}
      retry-base-seconds: ${MAIL_OUTBOX_RETRY_BASE_SECONDS:60}
      retry-max-seconds: ${MAIL_OUTBOX_RETRY_MAX_SECONDS:3600}
      processing-timeout-seconds: ${MAIL_OUTBOX_PROCESSING_TIMEOUT_SECONDS:300}
  storage:
    path: ${STORAGE_PATH:./data/uploads}
  coupons:
    claim-minutes: ${COUPON_CLAIM_MINUTES:15}
  automation:
    interval-ms: ${AUTOMATION_INTERVAL_MS:60000}
    initial-delay-ms: ${AUTOMATION_INITIAL_DELAY_MS:30000}
    reminder-lookback-minutes: ${REMINDER_LOOKBACK_MINUTES:10}
